#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include "../PC/PC.h"

int control_logic(char* instruction[]) {
    // since we got instruction we increment the PC
    goto_next_instruction();

    if (instruction is arithmtic type) {

        if (instruction is immidate type) {

        } else {


        }

    } else if (instruction is memory type) {
        switch(instruction type){

            case lordword:
                break;

            case storeword:
                break;

        }


    }

    else if (instruction is branch type) {
        switch (instruction type) {
            case:



        }

    } else if (instruction is jump type) {



    }



}